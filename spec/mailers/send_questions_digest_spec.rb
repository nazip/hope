require "rails_helper"

RSpec.describe SendQuestionsDigest, type: :mailer do
  # let(:user) { create(:user) }
  # let(:question) { create_list(:question, 2, user: user) }
  # let(:mail) { SendQuestionsDigest.send_email(user, question) }

  # it 'email body' do
  #   mail.body.encoded.should match(question)
  # end


  # it 'send emails with new questions daily' do
  #   User.all.each do |user|
  #     expect(SendQuestionsDigest).to receive(:send_email).with(user, question)
  #     SendQuestionsDigestJob.perform_now
  #   end
  # end
end
