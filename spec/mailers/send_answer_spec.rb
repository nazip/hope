require "rails_helper"

RSpec.describe SendAnswer, type: :mailer do
  # let(:answer) { create(:answer, question: question, user: user) }
  # it "send email with new answer to question's owner" do
  #   answer = Answer.new(question: question, user: user, body: 'new body')
  #   expect(SendAnswer).to receive(:send_email).with(user, answer)
  #   answer.save!
  #   # SendAnswerJob.perform_now(answer)
  # end
end
